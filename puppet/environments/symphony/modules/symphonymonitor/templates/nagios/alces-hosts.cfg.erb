
define host{
        use                     generic
        host_name               director
        alias                   director.bld
        address                 director
        }

define host{
        use                     generic
        host_name               repo
        alias                   repo.bld
        address                 repo
        }

# This machine is defined in the base nagios config (/etc/nagios/objects/cluster.cfg)
# so don't define it again.
#define host{
#        use                     generic
#        host_name               monitor
#        alias                   monitor.bld
#        address                 monitor
#        }

define host{
        use                     generic
        host_name               directory
        alias                   directory.bld
        address                 directory
        }

###################################################################################
# Cluster hosts
###################################################################################



define host{
        use                     generic
        host_name               meta1
        alias                   meta1.mgt.symphony.local
        address                 meta1
        }

define host{
        use                     generic
        host_name               meta2
        alias                   meta2.mgt.symphony.local
        address                 meta2
        }

define host{
        use                     generic
        host_name               hablock1-1
        alias                   hablock1-1.mgt.symphony.local
        address                 hablock1-1
        }

define host{
        use                     generic
        host_name               hablock1-2
        alias                   hablock1-2.mgt.symphony.local
        address                 hablock1-2
        }

define host{
        use                     generic
        host_name               headnode1
        alias                   headnode1.mgt.symphony.local
        address                 headnode1
        }

define host{
        use                     generic
        host_name               login1
        alias                   login1.mgt.symphony.local
        address                 login1
        }

define host{
        use                     generic
        host_name               master1
        alias                   master1.mgt.symphony.local
        address                 master1
        }

define host{
        use                     generic
        host_name               node01
        alias                   node01.mgt.symphony.local
        address                 node01
        }

define host{
        use                     generic
        host_name               node02
        alias                   node02.mgt.symphony.local
        address                 node02
        }

define host{
        use                     generic
        host_name               node03
        alias                   node03.mgt.symphony.local
        address                 node03
        }

define host{
        use                     generic
        host_name               node04
        alias                   node04.mgt.symphony.local
        address                 node04
        }

define host{
        use                     generic
        host_name               node05
        alias                   node05.mgt.symphony.local
        address                 node05
        }

define host{
        use                     generic
        host_name               node06
        alias                   node06.mgt.symphony.local
        address                 node06
        }

define host{
        use                     generic
        host_name               node07
        alias                   node07.mgt.symphony.local
        address                 node07
        }

define host{
        use                     generic
        host_name               node08
        alias                   node08.mgt.symphony.local
        address                 node08
        }

