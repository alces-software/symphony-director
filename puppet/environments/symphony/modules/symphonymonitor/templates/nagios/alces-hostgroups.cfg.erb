define hostgroup{
        hostgroup_name  symphonytrio
        alias           Symphony service nodes
        members         director,repo,monitor,directory
        }

#define hostgroup{
#        hostgroup_name generic
#        alias          Generic nodes
#        members
#}

define hostgroup{
        hostgroup_name  headnodes
        alias           Headnode systems
        members         headnode1
        }

define hostgroup{
        hostgroup_name  masters
        alias           Service master systems
        members         master1
        }

define hostgroup{
        hostgroup_name  lustremeta
        alias           Lustre metadata servers
        members         meta1,meta2
        }

define hostgroup{
        hostgroup_name  lustreoss
        alias           Lustre OSS nodes
        members         hablock1-1,hablock1-2
        }

define hostgroup{
        hostgroup_name  storage
        alias           Storage servers
        hostgroup_members       lustremeta,lustreoss
        }

define hostgroup{
        hostgroup_name  compute
        alias           Compute node systems
        members         node01,node02,node03,node04,node05,node06,node07,node08
        }

define hostgroup{
        hostgroup_name  logins
        alias           HPC Login node systems
        members         login1
        }

define hostgroup{
        hostgroup_name  symphony
        alias           Symphony nodes
        hostgroup_members symphonytrio
        }
