#cloud-config
system_info:
  default_user:
    name: symphony
    lock_passwd: true
    gecos: Symphony Administrator
    groups: [wheel, adm, systemd-journal]
    sudo: ["ALL=(ALL) NOPASSWD:ALL"]
    shell: /bin/bash
write_files:
-  content: |
     #Symphony config data
     CLUSTER=cluster
     ROOTPASSWORD="A1cesS0ftwar3"
     ADMINPASSWORD="A1cesS0ftwar3"
   path: /etc/symphony.cfg
   permissions: '0600'
   owner: root:root
-  content: |
     IPV6INIT=no
     BOOTPROTO=dhcp
     DEVICE=eth4
     ONBOOT=yes
     PEERDNS=yes
   path: /etc/sysconfig/network-scripts/ifcfg-eth4
   permissions: '0644'
   owner: root:root
